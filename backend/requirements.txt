# MAEF Backend — Python dependencies
# Pinned to minor-version ranges for reproducible Cloud Build

# Web framework
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0

# Blockchain / Web3
web3>=6.15.0,<7.0.0
eth-account>=0.10.0,<0.13.0
# eth-abi 6.0.0b1 (beta) has breaking API changes that crash web3 6.20.x at import time.
# Force a stable 4.x / 5.x release.
eth-abi>=4.0.0,<6.0.0a0

# GCP
google-cloud-secret-manager>=2.18.0,<3.0.0
google-cloud-firestore>=2.16.0,<3.0.0
google-cloud-kms>=2.21.0,<3.0.0
# OIDC token verification for Cloud Scheduler → Cloud Run auth
google-auth>=2.28.0,<3.0.0

# HTTP client (async, used by llm_service)
httpx>=0.26.0,<0.30.0

# YouTube transcript extraction (no API key required)
youtube-transcript-api>=0.6.0,<1.0.0

# Data validation
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0

# Security: pin vulnerable transitive deps to fixed versions
# (referenced by web3, google-auth, beautifulsoup4, etc.)
cryptography>=50.0.0,<60.0.0  # PYSEC-2026-3552 (transitive via web3)
anyio>=4.14.2,<5.0.0  # CVE-2026-63374, CVE-2026-64847 (transitive via httpx)
aiohttp>=3.14.3,<4.0.0  # PYSEC-2026-3545/3546/3547 (transitive via google-auth)
httplib2>=0.32.0,<1.0.0  # PYSEC-2026-3444 (transitive via google-api-python-client)
pyasn1>=0.6.4,<1.0.0  # PYSEC-2026-3455/3456/3457 (transitive via cryptography)
python-dotenv>=1.2.2,<2.0.0  # PYSEC-2026-2270 (transitive via pydantic-settings)
soupsieve>=2.9.0,<3.0.0  # CVE-2026-85999/86000 (transitive via beautifulsoup4)

# Test framework (not in production runtime)
pytest>=9.0.3,<10.0.0  # PYSEC-2026-1845
pytest-asyncio>=1.4.0,<2.0.0  # required by pytest>=9; older 0.x pins pytest<9
